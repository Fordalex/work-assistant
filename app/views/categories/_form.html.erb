<div>
<%= form_with(model: new_record) do |form| %>
  <% if new_record.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(new_record.errors.count, "error" ) %> prohibited this new_record from being saved:
      </h2>
      <ul>
        <% new_record.errors.each do |error| %>
          <li>
            <%= error.full_message %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= form.label :name %>
      <%= form.text_field :name, class: "form-control", required: true %>
    </div>

    <div class="form-group mb-3">
      <%= form.label :colour %>
      <%= form.color_field :colour, class: "form-control", required: true %>
    </div>

    <div class="actions">
      <%= form.submit :save, class: "btn btn-success container-fluid" %>
    </div>
<% end %>
</div>
