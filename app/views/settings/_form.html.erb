<div>
<%= form_with(model: setting) do |form| %>
  <% if setting.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(setting.errors.count, "error" ) %> prohibited this setting from being saved:
      </h2>
      <ul>
        <% setting.errors.each do |error| %>
          <li>
            <%= error.full_message %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="input-group-icon mb-3">
        <%= form.select :theme, Setting::ALLOWED_THEMES.map{|t| t}, {}, class: "form-control" %>
        <div class="input-icon">
          <i class="fas fa-palette"></i>
        </div>
      </div>

    <div class="actions">
      <%= form.submit :save, class: "btn btn-success container-fluid" %>
    </div>
<% end %>
</div>
