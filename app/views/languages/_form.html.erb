<div>
<%= form_with(model: new_record) do |form| %>
  <% if new_record.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(new_record.errors.count, "error" ) %> prohibited this language from being saved:
      </h2>
      <ul>
        <% new_record.errors.each do |error| %>
          <li>
            <%= error.full_message %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= form.label :name %>
      <%= form.text_field :name, class: "form-control", required: true %>
    </div>

    <div class="actions">
      <%= form.submit :save, class: "btn btn-success container-fluid mt-3" %>
    </div>
<% end %>
</div>
